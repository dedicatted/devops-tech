name: List Files

on:
  push:
   branches:
   - tf_modules
  workflow_dispatch:


jobs:
  list-files:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: List files
        run: ls -la
